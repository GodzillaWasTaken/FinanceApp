POSTGRES_USER=admin # Database admin username
POSTGRES_PASSWORD=password # Database password
POSTGRES_DB=finance_db # Database name
POSTGRES_HOST=database # Database service name (container name in docker-compose)
DJANGO_SECRET_KEY=django-insecure-key-replace-me # Secret key for Django security (change in production)
POSTGRES_PORT=5432 # Default PostgreSQL port